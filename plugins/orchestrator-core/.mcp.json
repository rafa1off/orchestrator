{
  "mcpServers": {
    "dev-tools": {
      "type": "stdio",
      "command": "bash",
      "args": [
        "-c",
        "uv run \"$HOME/.gemini/config/plugins/orchestrator-core/mcp-server-py/server.py\""
      ],
      "env": {
        "GEMINI_PROJECT_DIR": "${GEMINI_PROJECT_DIR}"
      }
    }
  }
}
