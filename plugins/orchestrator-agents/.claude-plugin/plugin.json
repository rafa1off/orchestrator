{
  "name": "orchestrator-agents",
  "version": "1.15.0",
  "description": "7-agent catalog for the orchestrator ecosystem: reader (haiku), researcher (sonnet), thinker (sonnet), writer (sonnet), checker (haiku), reviewer (sonnet), tester (sonnet, readonly run+diagnose). Leaf subagents — no nested Agent dispatch; readonly agents confined to their memory dir.",
  "author": {
    "name": "Rafael Giuriatto",
    "email": "rafael.giuriatto@resolva.ai"
  },
  "dependencies": [
    "orchestrator-mcp"
  ]
}
