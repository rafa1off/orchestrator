{
  "name": "orchestrator-mcp",
  "version": "1.3.0",
  "description": "Dev-tools MCP server for the orchestrator ecosystem \u2014 write_findings pipeline contract (checks + issues + failures schema) for the verify and tester agents. Rejects verify/tester findings with no checks[], and stamps written_at for freshness checks."
}
